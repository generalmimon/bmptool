{
  "name": "bmptool",
  "version": "0.1.0",
  "description": "Tool for working with BMP files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "kaitai-struct-compiler -t javascript -d dist resources/bmp.ksy && babel src --out-dir dist",
    "watch": "babel --watch src --out-dir dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.7.7",
    "@babel/core": "^7.7.7",
    "kaitai-struct": "github:kaitai-io/kaitai_struct_javascript_runtime#master",
    "kaitai-struct-compiler": "^0.8.0-SNAPSHOT.20180205.11616",
    "yargs": "^15.0.2"
  }
}
